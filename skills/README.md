# Custom Skill

`jixuncodec-rate-guard` is the project-specific rate consistency and stability gate. It identifies the active board rate, repeats the board benchmark, and checks timing variation and fingerprint consistency.

The skill source is included for review and installation. The contest still requires the official AI Coding log collector to export the real sessions into `logs/` before upload.
