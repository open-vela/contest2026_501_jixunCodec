############################################################################
# apps/jixun-codec/Make.defs
#
# 极讯 AI Codec 上板验证 app —— 无条件编入（不走 Kconfig，避免改动 .config
# 触发整树重编）。
############################################################################

CONFIGURED_APPS += $(APPDIR)/jixun-codec
